<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Permiso para acceso a Internet -->
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Taller1"
        tools:targetApi="31">

        <!-- Actividad principal -->
        <activity
            android:name=".Logica.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Otras actividades existentes en tu proyecto -->
        <activity
            android:name=".Logica.RecomendacionesActivity"
            android:exported="false" />
        <activity
            android:name=".Logica.FavoritosActivity"
            android:exported="false" />
        <activity
            android:name=".Logica.DetalleDestinoActivity"
            android:exported="false" />
        <activity
            android:name=".Logica.ExplorarDestinosActivity"
            android:exported="false" />
    </application>

</manifest>
